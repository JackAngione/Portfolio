# Local development config — points at the Docker dev stack (see mediaServer/README.md).
# Safe to commit: contains no real secrets.
MONGODB_CONNECTION_STRING=mongodb://localhost:27017/KNOWLEDGE
JWT_KEY=dev-only-jwt-key-not-for-production
MEILISEARCH_HOST=http://localhost:7700/
MEILISEARCH_MASTER_KEY=dev_master_key
